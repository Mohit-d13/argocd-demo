apiVersion: v1
kind: Service
metadata:
  name: postgresql-headless
  labels:
    app: shortener
    ver: "1.0"
    rel: canary
    tier: database
  annotations:
    argo.argoproj.io/sync-wave: "2"
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
      app: shortener
      tier: database
  ports:
  - name: postgresdb
    port: 5432
    targetPort: postgresdb
    protocol: TCP
