apiVersion: v1
kind: Service
metadata:
  name: shortener
  labels:
    app: shortener
    ver: "1.0"
    rel: canary
    tier: backend
  annotations:
    argo.argoproj.io/sync-wave: "4"
spec:
  selector:
    app: shortener
    tier: backend
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP