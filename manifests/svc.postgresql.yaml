apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: shortener
  labels:
    app: shortener
    rel: canary
    tier: database
  annotations:
    argo.argoproj.io/sync-wave: "3"
spec:
  selector:
    app: shortener
    tier: database
  ports:
  - name: postgresdb
    port: 5432
    targetPort: postgresdb
    protocol: TCP
